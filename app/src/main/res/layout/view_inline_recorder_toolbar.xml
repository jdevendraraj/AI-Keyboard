<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:background="#FF1E1E1E"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:paddingHorizontal="8dp">

    <!-- Back button (left) -->
    <ImageButton
        android:id="@+id/toolbar_back_btn"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/rounded_back_btn_bg"
        android:contentDescription="@string/voice_inline__back_accessibility"
        android:padding="8dp"
        android:src="@drawable/ic_arrow_back"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@android:color/white" />

    <!-- Center label -->
    <TextView
        android:id="@+id/toolbar_speak_now"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:ellipsize="end"
        android:gravity="center"
        android:text="@string/voice_inline__speak_now"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/recorder_container"
        app:layout_constraintStart_toEndOf="@id/toolbar_back_btn"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Recorder container (right) -->
    <FrameLayout
        android:id="@+id/recorder_container"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:clipChildren="false"
        android:clipToPadding="false"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Circular background -->
        <View
            android:id="@+id/recorder_background"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:background="@drawable/recorder_circle_bg" />

        <!-- Microphone icon with larger touch target -->
        <ImageButton
            android:id="@+id/recorder_icon"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="center"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/voice_inline__recorder_accessibility"
            android:padding="16dp"
            android:src="@drawable/ic_mic_rounded"
            app:tint="@android:color/white" />

        <!-- Language label overlay -->
        <TextView
            android:id="@+id/recorder_language_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/language_label_bg"
            android:paddingHorizontal="4dp"
            android:paddingVertical="2dp"
            android:text="EN"
            android:textColor="@android:color/white"
            android:textSize="10sp"
            android:textStyle="bold"
            android:translationX="-10dp"
            android:visibility="visible" />

        <!-- Processing spinner -->
        <ProgressBar
            android:id="@+id/recorder_spinner"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:visibility="gone"
            android:indeterminateTint="@android:color/white"
            style="?android:attr/progressBarStyle" />

        <!-- Cancel button (replaces microphone when processing) -->
        <ImageButton
            android:id="@+id/recorder_cancel"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="center"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/voice_inline__cancel_accessibility"
            android:padding="16dp"
            android:src="@drawable/ic_close_small"
            android:visibility="gone"
            app:tint="@android:color/white" />

    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
